--- crates/vault-client-rs/src/types/error.rs
+++ replace <impl From<ParseFloatError> for VaultError>::from -> Self with Default::default()
@@ -71,17 +71,17 @@
 impl From<ParseIntError> for VaultError {
     fn from(err: ParseIntError) -> Self {
         Self::Config(format!("integer parse error: {err}"))
     }
 }
 
 impl From<ParseFloatError> for VaultError {
     fn from(err: ParseFloatError) -> Self {
-        Self::Config(format!("float parse error: {err}"))
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl VaultError {
     #[must_use]
     pub fn is_retryable(&self) -> bool {
         match self {
             Self::Http(e) => e.is_timeout() || e.is_connect(),
